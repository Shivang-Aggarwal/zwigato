spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zwigatodb
    username: springstudent
    password: springstudent

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8090